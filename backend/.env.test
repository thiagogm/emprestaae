# Backend Test Environment Variables

# Server Configuration
NODE_ENV=test
PORT=3002

# Database Configuration (Test Database)
DATABASE_URL=mysql://test_user:test_password@localhost:3306/empresta_ae_test
DB_HOST=localhost
DB_PORT=3306
DB_USER=test_user
DB_PASSWORD=test_password
DB_NAME=empresta_ae_test

# JWT Configuration (Test keys)
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_SECRET=test-refresh-secret-key-for-testing-only
REFRESH_TOKEN_EXPIRES_IN=24h

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Redis Configuration (Test)
REDIS_URL=redis://localhost:6380

# File Upload Configuration
UPLOAD_DIR=test_uploads
MAX_FILE_SIZE=1048576

# Disable external services in test
AWS_S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
